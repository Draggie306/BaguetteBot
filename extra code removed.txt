global diceDiff
diceDiff = input("Set difficulty (minimum:1, max: Infinity):\n>>> ")
def dicegame(): #               dicegame
    global total
    global total2
    dice1 = random.randint(1,(int (diceDiff)))
    dice2 = random.randint(1,(int (diceDiff)))
    if dice1 == dice2: #        If dice are the same
        total = dice1+dice2
        total = total*2
        dice3 = random.randint(1,6)
        dice4 = random.randint(1,6)
        if dice3 == dice4: #    reroll same again (wow)
            total2 = dice3+dice4*total
        else: #                 reroll different
            total2 = dice3+dice4*total
    else: #                     If not the same
        total2 = dice1+dice2
        time.sleep(0.01)
        dicegame()
dicegame()

def messageforCustomDifficultyRandomiser():
  global hehemessage
  hehemessage = (str ("Difficulty: " +  (str (diceDiff)) + str (", Result: ") + str (total2)))
messageforCustomDifficultyRandomiser()

    if message.content.startswith('.rand2'):      #random from predifined       RAND2
        await message.channel.send(str (hehemessage))
        f = open("D:\\OneDrive - Sapientia Education Trust\\Year 10\\Computer Science\\Python\\draggiebot\\log.txt", "a")
        f.write((str ("\nINFO: '.rand2' ran by ")) + (str (message.author)) + (str (" at ") + (str (datetime.now()))))
        f.close()
        print ((str ("\nINFO: '.rand2' ran by ")) + (str (message.author)))
        #await message.channel.send
        dicegameNoPrint()